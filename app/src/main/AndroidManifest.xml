<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="ir.sadjadtalakoob.tarebar_shop">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.TareBarShop"
        android:usesCleartextTraffic="true">
        <activity
            android:name=".NavActivity"
            android:theme="@style/Theme.TareBarShop.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MainActivity"></activity>
        <activity android:name="com.example.senthil.kotlin_recyclerview.Activity.SearchRecyclerViewActivity">

        </activity>
        <activity android:name=".imageSlider">

        </activity>
    </application>

</manifest>